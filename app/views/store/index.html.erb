<div class="container text-center mt-2 border shadow backg border-success mt-4">
	<table class="mytable">
		
	</table>
	<!--
	<% @stores.each do |store| %>
		<div class="card mt-3 text-center shadow-lg mb-5 back mb-3" >
			<h5 class="card-header"><%= store.name %>
		<table class="table table-sm shadow back" >
			<thead>
				<% store.categories.keys.each do |cat| %>
				<tr>
					<th scope="col"><%= cat %></th>
						<% store.categories[cat].each do |p|%>
							<td scope="row"><%= p[0] %></td>
						<% end %>
				</tr>
				<% end %>
			</thead>
		</table>
		<% if user_signed_in? && current_user.is_admin %>
			<button class="show-store-btn" data-id="<%= store.id %>">Show Store</button>
			<%= link_to 'edit Store', edit_store_path(store.id), class: "btn btn-info mb-2 shadow"%>
			<%= link_to 'Delete Store', store, class: "btn btn-danger shadow mb-2", method: :delete %>
		<% end %>
		</div>
	<% end %>

	<div class="container  text-center">
		<% if user_signed_in? && current_user.is_admin %>
			<%= link_to 'Create Store', new_store_path, class: "btn btn-success shadow btn-lg mb-2", method: :get %>
		<% end 	%>	
	</div>
		-->
		
</div>
<% if user_signed_in? && current_user.is_admin %>
	<%= link_to 'Create Store', new_store_path, class: "btn btn-success shadow btn-lg mb-2", method: :get %>
<% end 	%>	